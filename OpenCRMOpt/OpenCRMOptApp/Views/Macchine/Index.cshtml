@model IEnumerable<OpenCRMOptModels.Macchine>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model!.First().MacchineId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model!.First().Descrizione)
            </th>
            <th>
                @Html.DisplayNameFor(model => model!.First().Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model!.First().Ip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.MacchineId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descrizione)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ip)
                </td>
                @* <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td> *@
            </tr>
        }
    </tbody>
</table>